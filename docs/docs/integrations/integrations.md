# Chekt Integration

Enable in config with `chekt: enabled: true`. Configure global settings and per-camera channel numbers via UI or YAML.

Defaults: 20s rate limit, 10s video. Bounding boxes drawn by default.

Test via Settings > Chekt tab.